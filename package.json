{
  "name": "heribertolord96.github.io",
  "homepage": "https://heribertolord96.github.io",
  "private": true,
  "version": "1.0.0",
  "description": "Personal portfolio — static compact dashboard (dark IDE UI)",
  "scripts": {
    "dev": "node scripts/build.mjs && npx --yes serve dist -l 3000",
    "start": "npm run dev",
    "build": "node scripts/build.mjs",
    "preview": "npx --yes serve dist -l 4173",
    "predeploy": "npm run build",
    "deploy": "npx --yes gh-pages -d dist",
    "dev:vite": "vite",
    "build:vite": "vite build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
